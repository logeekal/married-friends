{
  "name": "mf",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@netlify/functions": "^0.7.2",
    "axios": "^0.21.1",
    "gatsby": "3.1.2",
    "global": "^4.4.0",
    "mf-blog-theme": "*",
    "netlify-cli": "^3.29.17",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "scripts": {
    "start": "gatsby develop",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public",
    "build": "gatsby build",
    "serve": "gatsby build && serve -s public"
  },
  "devDependencies": {
    "gh-pages": "^2.2.0"
  }
}
